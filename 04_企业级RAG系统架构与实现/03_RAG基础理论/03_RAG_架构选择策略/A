一.如何选型？
在实际工程中，我们不应“一刀切”地使用某一种 RAG，而应根据用户查询的特性动态选择架构。下图展示了一个典型的决策流程：
作为工程师，我们的任务不是选择“最好的 RAG”，而是构建最适合业务的 RAG —— 灵活、可靠、可进化

二.各分支详解：
✅ 1. 简单 RAG（对应 Standard RAG）
    特点：固定分块 + 单次向量检索 + 直接生成
    技术栈：SentenceTransformer + FAISS + LLM
    适用：FAQ、产品手册问答等低复杂度场景

✅ 2. 多轮对话 RAG
    挑战：需理解对话历史，避免重复或断层
    关键技术：
        对话状态跟踪（DST）
        历史压缩（如只保留最近3轮）
        Query Rewriting（将当前问句补全上下文）
示例：
用户：“它支持哪些格式？” → 系统重写为：“Llama.cpp 支持哪些模型格式？”

✅ 3. 多步骤 RAG（Iterative RAG）
    适用：复合问题（如“比较 A 和 B 的优缺点，并给出建议”）
    流程：
        LLM 将问题拆解为子问题
        分别检索每个子问题
        合并结果后生成最终答案
        优势：提升复杂问题的准确率与可解释性

✅ 4. 混合检索 RAG（Hybrid RAG）
    为什么需要？
        向量检索擅长语义匹配，但对术语、数字不敏感
        关键词检索（如 BM25）擅长精确匹配

    融合策略：
    Reciprocal Rank Fusion（RRF）
        加权打分（如 0.6×语义 + 0.4×关键词）
    工具组合：ElasticSearch + Pinecone

✅ 5. 重排序 RAG（Advanced RAG）
    问题：向量检索 Top-K 可能包含噪声
    解决方案：用 Cross-Encoder（如 bge-reranker-large）对初检结果重新打分
    效果：MRR（平均倒数排名）显著提升，尤其在长尾查询上

三.进阶能力：让 RAG 更智能
除了上述主干架构，现代 RAG 还引入了两类关键机制：

🔒 1. Self-RAG：是否需要检索？
    LLM 先判断：“我能直接回答吗？”
    若能（如常识问题），跳过检索，节省资源
    若不能，才启动检索流程
    价值：提升响应速度，降低计算成本

🛡️ 2. CRAG：结果可信吗？
    在生成后增加“验证环节”：
        检查事实一致性
        对比多个来源
        必要时触发外部 API（如维基百科、数据库）
    目标：杜绝“一本正经地胡说八道”