# 🚀 容器化部署与编排复习笔记

基于 LegalMind 项目，覆盖 FastAPI、Nginx、Supervisor、Docker Compose、Kubernetes、GitLab CI/CD 全链路知识体系。

> 本笔记聚焦概念、关系和流程，不包含具体代码实现。

## 📚 目录

- [一、各组件层级定义与关系](#一各组件层级定义与关系)
- [二、核心组件详解](#二核心组件详解)
  - [1. FastAPI + Gunicorn + Uvicorn](#1-fastapi--gunicorn--uvicorn)
  - [2. Nginx 的核心功能](#2-nginx-的核心功能)
  - [3. Supervisor 的位置与用法](#3-supervisor-的位置与用法)
  - [4. Docker Compose 的核心价值](#4-docker-compose-的核心价值)
  - [5. Kubernetes 解决了什么问题](#5-kubernetes-解决了什么问题)
  - [6. GitLab CI/CD 的完整流程](#6-gitlab-cicd-的完整流程)
- [三、各环境部署方案对比](#三各环境部署方案对比)
- [四、核心概念对比速查](#四核心概念对比速查)
- [五、部署前准备工作清单](#五部署前准备工作清单)
- [六、快速记忆口诀](#六快速记忆口诀)
- [七、常见故障排查方向](#七常见故障排查方向)

---

## 1️⃣ 各组件层级定义与关系

层级划分（从内到外）：

| 层级 | 组件 | 管理对象 | 核心职责 | 生活类比 |
| --- | --- | --- | --- | --- |
| 进程级 | Supervisor | 单个进程 | 进程挂了自动拉起来 | 保姆 |
| 应用服务器级 | Gunicorn + Uvicorn | FastAPI 应用 | 管理多进程，处理 HTTP 请求 | 包工头 + 工人 |
| 流量入口级 | Nginx | HTTP/HTTPS 流量 | 反向代理、SSL、静态文件、限流 | 大门保安 |
| 容器级（单机） | Docker Compose | 多个容器 | 单机编排，一键启动所有服务 | 物业管家 |
| 容器级（集群） | Kubernetes | 跨机器的海量容器 | 集群调度、自愈、自动伸缩 | 市政府 |
| 自动化流水线 | GitLab CI/CD | 代码到部署全流程 | 自动测试、构建镜像、部署 | 自动化工厂 |

---

## 2️⃣ 核心组件详解

### 2.1 FastAPI + Gunicorn + Uvicorn

- **FastAPI**：业务逻辑和路由定义。
- **Uvicorn**：ASGI 服务器，单进程，适合开发调试。
- **Gunicorn**：多进程管理器，启动多个 Uvicorn Worker，实现多核利用。

#### 🔍 为什么生产环境必须用 Gunicorn？

- Uvicorn 单进程，无法利用多核 CPU。
- Gunicorn 管理多个 Worker，提升并发能力。
- 每个 Worker 独立处理请求，稳定性更好。

#### ⚙️ 开发模式 vs 生产模式

- 开发：直接用 Uvicorn，方便热加载调试。
- 生产：用 Gunicorn 托管多个 Uvicorn Worker。

### 2.2 Nginx 的核心功能

Nginx 是流量入口，生产环境承担：

- ✅ 反向代理：转发 `/api/*` 请求给 FastAPI。
- ✅ SSL/TLS 终止：统一管理 HTTPS，后端只需 HTTP。
- ✅ 静态文件服务：直接返回前端资源。
- ✅ 负载均衡：后端多个实例时分发请求。
- ✅ 限流与缓冲：防止慢客户端耗尽后端连接。
- ✅ 超时控制：SSE 需要拉长超时时间，如 300 秒。

#### 💡 关键配置点

- SSE 流式响应需关闭缓冲、拉长超时。
- 代理转发时要传递真实客户端 IP。

### 2.3 Supervisor 的位置与用法

| 部署方式 | Supervisor 安装位置 | 管理对象 | 是否需要 |
| --- | --- | --- | --- |
| 传统部署（无容器） | 宿主机 | Gunicorn、Celery、Nginx 等进程 | ✅ 需要 |
| 容器化部署（单容器多进程） | 容器内 | 多个进程（Gunicorn + Celery） | ⚠️ 可选 |
| Kubernetes 环境 | 无 | 无 | ❌ 不需要 |

#### ✅ 何时需要 Supervisor

- 传统服务器部署，需要守护多个进程。
- 单个容器内必须运行多个进程时（不推荐）。

#### ❌ 何时不需要 Supervisor

- Kubernetes 环境：`livenessProbe` / `readinessProbe` 已替代。
- 每个容器只跑一个进程时，推荐直接运行该进程。

#### 🔄 Supervisor vs K8s 探针

- Supervisor：容器内部监控进程，进程挂了就重启，容器继续运行。
- K8s 探针：容器外部检测，失败时重启整个 Pod。

### 2.4 Docker Compose 的核心价值

#### 🎯 适用对象

- 本地开发：一键启动所有依赖。
- CI/CD 测试环境：快速构建集成环境。
- 小型生产项目：单机部署。

#### 🧩 解决问题

- 不用 Compose 时，需要手动按顺序启动 PostgreSQL、Redis、后端、Nginx。
- Compose 让一条命令完成启动，容器名自动发现服务。

#### 🔧 核心概念

- 服务名互通：如 `backend` 访问 `postgres:5432`。
- 持久化：数据卷保留数据，容器删掉也不丢失。
- 健康检查：`healthcheck` 保证依赖就绪后再启动主服务。

### 2.5 Kubernetes 解决了什么问题

#### 🔁 K8s 替代了什么

| 被替代的工具 | K8s 对应组件 | 说明 |
| --- | --- | --- |
| Docker Compose | Deployment + Service + ConfigMap | 单机编排 → 集群编排 |
| Supervisor | livenessProbe / readinessProbe | 进程守护 → Pod 生命周期管理 |
| 单机 Nginx 反向代理 | Ingress Controller | 更强流量管理 |
| 手动负载均衡 | Service（ClusterIP/LoadBalancer） | 自动服务发现与 LB |

#### 🚀 K8s 核心能力

- 自愈：容器挂了自动重启，节点挂了重新调度。
- 滚动更新：先起新 Pod，再停旧 Pod，减少停机。
- 自动伸缩：基于 CPU / 内存自动扩缩容。
- 服务发现：Service 自动做负载均衡和 DNS。

#### 🧠 为什么 K8s 还要 Nginx（Ingress）？

K8s Service 只能解决“找到 Pod”的问题，无法解决：

| 需求 | K8s Service | 由谁处理 |
| --- | --- | --- |
| 多服务共用同一 IP/端口 | ❌ | Ingress |
| HTTPS/SSL 证书统一管理 | ❌ | Ingress |
| 域名/路径路由 | ❌ | Ingress |
| 限流、IP 白名单、路径重写 | ❌ | Ingress |
| SSE/WebSocket 超时控制 | ❌ | Ingress |

- K8s Service = 电话本。
- Nginx Ingress = 总机。

> 没有 Ingress，服务需要独立 LoadBalancer，成本高且管理混乱。

### 2.6 GitLab CI/CD 的完整流程

| 阶段 | 关键动作 | 作用 |
| --- | --- | --- |
| 测试 | 单元测试、代码风格、安全扫描 | 保证质量，避免坏代码进入生产 |
| 构建 | 打包镜像、推送仓库 | 生成可运行容器镜像 |
| 部署 | 连接目标机、执行部署命令 | 真正将应用上线 |

#### 🌐 部署方式灵活性

- GitLab 只触发流水线，部署逻辑由你写。
- 想用 Compose 就用 Compose，想用 K8s 就用 `kubectl`。
- 同一项目可以测试环境用 Compose，生产环境用 K8s。

---

## 3️⃣ 各环境部署方案对比

| 环境 | 工具 | 复杂度 | 场景 |
| --- | --- | --- | --- |
| 本地开发 | Uvicorn + 手动数据库 | 低 | 代码调试 |
| 开发联调 | Docker Compose 全服务 | 中 | 模拟生产 |
| 测试环境 | Docker Compose + GitLab CI | 中 | 自动化测试 |
| 生产（传统） | 宿主机 Nginx + Gunicorn + Supervisor | 高 | 无容器化维护 |
| 生产（容器化） | K8s + Nginx Ingress | 高 | 高可用、滚动更新 |

---

## 4️⃣ 核心概念对比速查

### Docker Compose vs Kubernetes

| 对比项 | Docker Compose | Kubernetes |
| --- | --- | --- |
| 配置文件 | 单个 YAML | 多个 YAML |
| 范围 | 单机 | 集群 |
| 自动伸缩 | ❌ | ✅ |
| 自愈能力 | 容器重启 | Pod 重调度 |
| 滚动更新 | ❌ | ✅ |
| 适用场景 | 开发、小项目 | 生产、大项目 |

### Supervisor vs K8s 探针

| 对比项 | Supervisor | K8s 探针 |
| --- | --- | --- |
| 运行位置 | 容器内 / 宿主机 | 控制平面检测 |
| 管理对象 | 进程 | Pod |
| 重启方式 | 重启进程 | 重启容器 |
| 推荐场景 | 传统部署 | K8s |

### 独立 Nginx vs Nginx Ingress

| 对比项 | 独立 Nginx 容器 | Nginx Ingress |
| --- | --- | --- |
| 运行位置 | Compose 容器 | K8s Pod |
| 配置方式 | `nginx.conf` 挂载 | Ingress YAML |
| 更新方式 | 需 reload | 动态更新 |
| K8s 集成 | 手动 | 原生 |

---

## 5️⃣ 部署前准备工作清单

1. 环境变量与隔离
   - 多套配置：开发 / 测试 / 生产。
   - `.env` 管理数据库、Redis、密钥、LLM API、前端地址等。

2. 编写 Dockerfile
   - 后端：多阶段构建，减小镜像体积。
   - 生产镜像用 Gunicorn 启动，不是直接用 Uvicorn。
   - 区分构建阶段和运行阶段。
   - 前端：构建静态文件，运行时用 Nginx 提供服务。

3. 数据库初始化
   - 运行初始化脚本：建表、插入种子数据、构建向量索引。
   - 建议放在 CI/CD 预部署步骤。

4. 健康检查接口
   - 提供 `/health`，返回 `200`，供 `healthcheck` 和探针使用。

5. 日志配置
   - 输出到 stdout，由平台统一收集。
   - 生产建议接入 ELK / Loki，方便排查。

6. 网络规划
   - Compose：自定义网络，服务名互通。
   - K8s：使用 `Namespace` 隔离，配置 `NetworkPolicy`。

7. SSL 证书准备
   - 生产提前准备域名和证书。
   - Compose：挂载证书到 Nginx 容器。
   - K8s：证书保存为 Secret，Ingress 引用。

---

## 6️⃣ 快速记忆口诀

- 请求流转路径：
  用户 → Nginx（拦路收发/发静态文件） → Gunicorn（包工头叫小弟） → Uvicorn（小弟跑腿） → FastAPI（干活的） → PostgreSQL/Redis（查资料/记笔记）

- 部署工具层级：
  - Supervisor = 保姆（管进程）
  - Gunicorn = 包工头（管 Worker 数量）
  - Nginx = 保安（管流量入口）
  - Docker Compose = 物业管家（管一栋楼的容器）
  - K8s = 市政府（管整个城市的容器）
  - GitLab CI/CD = 自动化施工队（管盖楼和交房）

- K8s 为什么还要 Nginx：
  - K8s Service = 电话本（找到 Pod 的 IP）
  - Nginx Ingress = 总机（根据域名/路径转接）
  - 没有总机，每个部门都要装公用电话（LoadBalancer），又贵又乱。

---

## 7️⃣ 常见故障排查方向

### Docker Compose 环境

- ❌ 容器启动失败：查看日志。
- ⚠️ 端口冲突：检查宿主机端口。
- 🔎 依赖服务未就绪：检查 `healthcheck`。
- 🌐 容器间无法通信：确认是否在同一网络。

### Kubernetes 环境

- ❌ Pod 启动失败：用 `kubectl describe` 查看详情。
- ⚠️ 镜像拉取失败：检查镜像地址与认证。
- 🔎 探针失败：检查健康接口。
- 🌐 Service 无法访问：检查 selector 与标签。
- 🚦 Ingress 不生效：检查域名解析和规则配置。
