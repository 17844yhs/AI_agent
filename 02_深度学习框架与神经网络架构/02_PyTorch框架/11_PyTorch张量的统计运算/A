统计运算
    mean( ) 计算张量元素的均值
    sum( ) 计算张量元素的总和
    max( ) 返回张量中的最大值及其索引
    min( ) 返回张量中的最小值及其索引
    median( ) 元素的中位数
    std( ) 计算标准差
    var( ) 元素的方差
    mode( ) 众数和对应索引
参数:
dim参数：
    dim=0：按列计算（垂直方向）
    dim=1：按行计算（水平方向）
    不指定dim：计算所有元素
调用方式
    函数形式：torch.function(tensor)
    方法形式：tensor.function()