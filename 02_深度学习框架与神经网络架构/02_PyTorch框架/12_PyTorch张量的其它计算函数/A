计算函数
    sqrt() 计算张量每个元素的平方根
    exp() 计算张量每个元素的指数值 (e^x)
    log() 计算张量每个元素的自然对数 (ln(x))
    log2() 计算张量每个元素以2为底的对数
    log10() 计算张量每个元素以10为底的对数
    abs() 计算张量中每个元素的绝对值
    ceil() 对张量中每个元素向上取整
    floor() 对张量中每个元素向下取整

常见应用场景
    sqrt()：计算标准差、距离等
    exp()：激活函数、概率计算
    log()：信息熵计算、对数似然
    abs()：误差计算
    ceil()/floor()：索引处理、离散化