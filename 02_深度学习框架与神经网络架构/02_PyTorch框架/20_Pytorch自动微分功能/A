PyTorch 的 自动微分模块（Autograd） 是框架的核心组件之一，它使得神经网络的训练过程能够自动计算梯度并反向传播更新参数。

Autograd 的设计基于动态计算图，其灵活性和高效性使得 PyTorch 成为深度学习研究中的热门工具。

训练神经网络时，最常用的算法就是反向传播与梯度计算。在该算法中，参数(模型权重)会根据损失函数关于对应参数的梯度进行调整。

自动微分模块就可以自动梯度的计算，无需手动推导和编码梯度，也可以自动计算张量操作的导数。

图解释:
    图中展示了一个前向计算过程：从输入x开始，经过乘法(*)、加法(+)等运算得到$\hat{y}$，然后计算MSE损失
    w和b是模型参数，显示为虚线框中的节点
    y应该是真实标签，用于计算MSE误差
    最后得到loss作为整个网络的损失值