一.点乘
    定义： 点乘是指两个张量对应位置的元素相乘，得到一个新的张量，新张量的形状与原始张量相同。
    符号： 通常使用 * 运算符进行点乘操作 (或者使用torch.mul()函数)
    前提条件:两个张量的形状必须完全相同，或者满足广播机制
注意:
不是数学意义上的"点积"，而是执行的是元素级别的乘法

二.矩阵乘法
定义： 矩阵乘法是线性代数中定义的矩阵乘法运算。
    如果 A 是一个 $ m × n $ 的矩阵，B 是一个 $ p × q $ 的矩阵，那么 A 和 B 可以进行矩阵乘法的条件是 $ n = p$，结果 C 将是一个 $ m × q $ 的矩阵。
    矩阵乘法的每个元素是通过将 A 的行与 B 的列进行点积得到的。
符号：
    使用 torch.matmul() 或者 @ 符号来执行矩阵乘法
    torch.mm() 函数只能做二维矩阵的乘法，torch.matmul()支持广播，可以做更高维的矩阵乘法操作。所以推荐使用 torch.matmul()函数。

三.点乘和矩阵乘法的区别：
特性	点乘	矩阵乘法
定义	张量对应位置元素相乘，结果形状与输入张量相同	矩阵按线性代数规则相乘，需要满足维度要求
运算符	* 或 torch.mul()	torch.matmul() 或 @
形状要求	形状完全相同或满足广播规则	遵守矩阵乘法的维度要求；例如（m×n）与（n×p）矩阵相乘
结果形状	与输入张量形状相同	$ m×p$ 形状
应用场景	特征缩放，逐元素掩码，各种损失函数	神经网络的线性层，向量变换