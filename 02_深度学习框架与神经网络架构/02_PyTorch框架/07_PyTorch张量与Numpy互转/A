一.为什么张量与Numpy类型转换
NumPy 是 Python 中用于科学计算的核心库，拥有丰富的功能，如多维数组操作、数学函数和线性代数等。
    处理某些特定类型的数据（如图像、音频）时可能更方便。例如将一个图片数据转换成Numpy格式后, 可以很方便的调用各种OpenCV图像处理库
    NumPy 有更成熟的数组操作方法和更丰富的生态，可以更方便的进行数据分析
PyTorch 张量通常在 GPU 上进行快速计算,而 NumPy 数组主要用于 CPU 计算

二.张量转换为NumPy数组
使用 tensor.numpy( ) 方法：张量提供了 .numpy() 方法，用于将张量转换为 NumPy 数组

注意
    张量必须在 CPU 上才能转换为 NumPy 数组。如果在 GPU 上，首先需要使用 .cpu() 方法将其移至 CPU
    转换后的 NumPy 数组和原始张量共享内存，修改其中一个，另一个也会被修改, 若不想修改可以使用.copy函数

三.NumPy数组 转换 为张量
    使用 torch.from_numpy() 方法： 可以将 NumPy 数组转换为 PyTorch 张量。共享内存
    使用torch.tensor(ndarray)方法：可以将 NumPy 数组转换为 PyTorch 张量。不共享内存

四.标量张量和数字的转换
    使用 .item() 方法： 对于只有一个元素的标量张量，可以使用 .item() 方法将其转换为 Python 的数字
    注意：item() 函数只能用在标量张量上，如果不是标量，会报错

    使用torch.tensor() 函数: 可以将 Python 的数字或者变量直接转换成标量张量