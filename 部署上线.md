# 部署方案对比（复习速查）

> **一句话选型**：小项目用 PaaS/Serverless → 大了再上 K8s，别提前折腾。

## 选型速查表

| 方案 | 成本 | 运维 | 高可用 | 适合场景 |
|------|------|------|--------|---------|
| **A. 纯单机** | ⭐ 低 | ⭐ 低 | ❌ | 内部工具、日活<1k |
| **B. Docker Compose** | ⭐⭐ 中 | ⭐⭐ 中 | ❌ | 小团队、需环境一致 |
| **C. K8s 集群** | ⭐⭐⭐⭐⭐ 高 | ⭐⭐⭐⭐⭐ 高 | ✅ | 大流量、需高可用 |
| **D. PaaS/Serverless** | ⭐ 按量 | ⭐ 低 | ✅ 平台保障 | 初创、中小团队首选 |

---

## 方案 A：纯单机部署

```
Nginx(80/443) → Gunicorn(8000) → Supervisor(管理)
MySQL(3306) + Redis(6379)  ← localhost 仅本机访问
```

- ✅ 最省钱、够简单
- ❌ 单点故障、扩容靠手动

## 方案 B：Docker Compose

```
Nginx容器 → FastAPI容器 → MySQL容器 + Redis容器
所有服务一个 docker-compose.yml 编排
```

- ✅ 环境一致、一键启停、易迁移
- ❌ 单点故障、需学 Docker

## 方案 C：K8s 集群

```
Ingress → Service → Deployment(Pod×3, 分布在多节点)
StatefulSet: MySQL主从 + Redis集群
```

- ✅ 高可用、自动伸缩、滚动更新、自愈
- ❌ 成本高（3台起）、学习曲线陡、需专职运维

## 方案 D：PaaS / Serverless（2026 主流推荐）

```
代码/镜像 → PaaS平台(Railway/Render/Fly.io/Cloud Run)
平台托管：自动TLS、域名、扩缩容
托管DB：PostgreSQL/Redis 即开即用
```

- ✅ 零管理服务器、按量付费、部署极快、自动伸缩
- ❌ 供应商锁定、有冷启动、不适合长时间任务
- 💡 日请求 50 万以下性价比优于 K8s

---

## 2026 关键趋势

- **K8s 在退潮**：42% 团队从微服务回退，90% 团队其实不需要 K8s
- **PaaS 成熟了**：Railway/Render/Fly.io 已具备自动伸缩、多区域部署、内置 DB
- **Serverless + K8s 融合**：Knative/KEDA（可缩容到 0）让二者不再二选一
- **Wasm 崛起**：毫秒级启动，轻量级容器替代方案

> **核心原则**：从简单开始，痛点真实存在时再加复杂度。